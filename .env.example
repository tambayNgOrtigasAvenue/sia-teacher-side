# ===========================================
# Docker Environment Configuration
# ===========================================
# Copy this file to .env and update with your values
# NEVER commit .env to version control!

# ===========================================
# MySQL Database Configuration
# ===========================================
DB_ROOT_PASS=rootpassword123
DB_NAME=gymnadb
DB_USER=gymnazo_user
DB_PASS=gymnazo_pass_2024

# ===========================================
# Database Connection (For Backend)
# ===========================================
# Use 'localhost' if backend runs on XAMPP (current setup)
# Use 'mysql' if backend runs in Docker container (full setup)
DB_HOST=localhost
DB_PORT=3306

# ===========================================
# Application URLs (For Full Docker Setup)
# ===========================================
API_URL=http://localhost:8080
FRONTEND_URL=http://localhost:5173
PHPMYADMIN_URL=http://localhost:8081

# ===========================================
# Security (Production)
# ===========================================
# Generate secure passwords for production:
# PowerShell: [System.Web.Security.Membership]::GeneratePassword(32, 8)
APP_ENV=development
APP_DEBUG=true
ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# Email Configuration (Optional)
# ===========================================
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@gymnazo.edu
MAIL_FROM_NAME="Gymnazo Christian Academy"
